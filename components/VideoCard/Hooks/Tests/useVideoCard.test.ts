import { renderHook } from "@testing-library/react";
import { useVideoCard } from "../useVideoCard";

describe("useVideoCard", () => {
  it("works correctly", () => {
    expect(true).toBe(true);
  });
});

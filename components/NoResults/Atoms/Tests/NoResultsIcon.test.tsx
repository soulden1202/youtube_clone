import React from "react";
import { render } from "@testing-library/react";
import NoResultsIcon from "../NoResultsIcon";

describe("NoResultsIcon", () => {
  it("renders correctly", () => {
    expect(true).toBe(true);
  });
});

import { renderHook } from "@testing-library/react";
import { usePlayListQueue } from "../usePlayListQueue";

describe("usePlayListQueue", () => {
  it("works correctly", () => {
    expect(true).toBe(true);
  });
});

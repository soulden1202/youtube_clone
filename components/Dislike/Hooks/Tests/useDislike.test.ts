import { renderHook } from "@testing-library/react";
import { useDislike } from "../useDislike";

describe("useDislike", () => {
  it("works correctly", () => {
    expect(true).toBe(true);
  });
});

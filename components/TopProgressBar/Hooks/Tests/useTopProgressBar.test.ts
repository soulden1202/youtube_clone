import { renderHook } from "@testing-library/react";
import { useTopProgressBar } from "../useTopProgressBar";

describe("useTopProgressBar", () => {
  it("works correctly", () => {
    expect(true).toBe(true);
  });
});

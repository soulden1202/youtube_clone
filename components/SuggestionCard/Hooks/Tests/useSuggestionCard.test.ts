import { renderHook } from "@testing-library/react";
import { useSuggestionCard } from "../useSuggestionCard";

describe("useSuggestionCard", () => {
  it("works correctly", () => {
    expect(true).toBe(true);
  });
});

import { renderHook } from "@testing-library/react";
import { useSideBar } from "../useSideBar";

describe("useSideBar", () => {
  it("works correctly", () => {
    expect(true).toBe(true);
  });
});

import { renderHook } from "@testing-library/react";
import { usePlayListQueueContainer } from "../usePlayListQueueContainer";

describe("usePlayListQueueContainer", () => {
  it("works correctly", () => {
    expect(true).toBe(true);
  });
});
